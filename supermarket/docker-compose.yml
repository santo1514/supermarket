services:
  supermarket:
    build: supermarket
    mem_limit: 512m
    ports:
      - "8080:8080"
    environment:
      SERVER_PORT: 8080
    volumes:
      - h2-data:/app/data
    restart: always

volumes:
  h2-data: